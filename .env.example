# Public JSON endpoint used by the mobile app to load paper metadata.
# Host this manifest in your backend and point PDF URLs to UploadThing file URLs.
EXPO_PUBLIC_MANIFEST_URL=http://localhost:3000/api/mobile/manifest
